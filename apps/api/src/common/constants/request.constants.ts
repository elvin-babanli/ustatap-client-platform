/**
 * Request context constants.
 * Used for attaching request-scoped data (e.g. requestId).
 */

export const REQUEST_ID_HEADER = "x-request-id" as const;
export const REQUEST_CONTEXT_KEY = "requestId";
